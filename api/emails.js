module.exports=async(req,res)=>{res.setHeader('Access-Control-Allow-Origin','*');if(req.method==='GET')return res.status(200).json({emails:(process.env.DEFAULT_EMAILS||'').split(',').map(x=>x.trim()).filter(Boolean)});const emails=(req.body&&req.body.emails||[]).map(x=>String(x).trim().toLowerCase());if(!emails.length||emails.length>5||emails.some(x=>!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(x))||new Set(emails).size!==emails.length)return res.status(400).json({error:'Use 1-5 unique valid email addresses'});return res.status(200).json({ok:true,emails});};
